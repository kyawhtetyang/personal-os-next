# Discovery

Discovery contains the canonical machine-readable entry point for understanding Personal OS Next.

Start with:

```text
system/discovery/manifest.json
```

The manifest points to architecture, version, capability discovery, contracts, and runtime entrypoints.
