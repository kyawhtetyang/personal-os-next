# Vault

Human-facing workspace.

Primary interface: Obsidian.

Contains human-authored notes, journals, projects and knowledge.

Runtime code and generated machine artifacts should not be mixed into the vault by default.